body {
	background: @background;

	.bg-gradient {
		content: '';
		display: block;
		// position: absolute;
		// top: 0;
		// left: 0;
		// right:0;
		// bottom: 0;
		#gradient.radialCust(rgba(255,255,255,.2), rgba(255,255,255,.05), 400px, center, top);
	}
}

